diff --git a/tests/conftest.py b/tests/conftest.py
index db9eda9..70ff59c 100644
--- a/tests/conftest.py
+++ b/tests/conftest.py
@@ -373,6 +373,21 @@ def protect_get_requires(fp, monkeypatch):
 
     monkeypatch.setattr(importlib.util, "find_spec", find_spec)
 
+@pytest.fixture()
+def setup_macos_tools(fp, monkeypatch):
+    """Auto-register macOS development tools for all tests"""
+    if sys.platform == "darwin":
+        # Register lipo with wildcard matching for any binary path
+        fp.register(
+            ["lipo", "-info", fp.any()], 
+            stdout="Non-fat file: program is architecture: arm64"
+        )
+
+        # Mock the function to always return False (not x86)
+        #monkeypatch.setattr(
+        #    "scikit_build_core.program_search.macos_binary_is_x86",
+        #    lambda path: False
+        #)
 
 def pytest_collection_modifyitems(items: list[pytest.Item]) -> None:
     for item in items:
diff --git a/tests/test_cmake_config.py b/tests/test_cmake_config.py
index 6a86e1b..e352576 100644
--- a/tests/test_cmake_config.py
+++ b/tests/test_cmake_config.py
@@ -238,7 +238,7 @@ def test_cmake_defines(
         """
     )
 
-
+@pytest.mark.usefixtures("setup_macos_tools")
 def test_get_cmake_via_envvar(monkeypatch: pytest.MonkeyPatch, fp):
     monkeypatch.setattr("shutil.which", lambda x: x)
     cmake_path = Path("some-prog")
diff --git a/tests/test_get_requires.py b/tests/test_get_requires.py
index 58d4a36..a22ae40 100644
--- a/tests/test_get_requires.py
+++ b/tests/test_get_requires.py
@@ -25,7 +25,7 @@ def protect_get_requires_autouse(protect_get_requires: None):
     Autouse this fixture in this test.
     """
 
-
+@pytest.mark.usefixtures("setup_macos_tools")
 def test_get_requires_parts(fp: FakeProcess):
     fp.register(
         [Path("cmake/path"), "-E", "capabilities"],
@@ -34,7 +34,7 @@ def test_get_requires_parts(fp: FakeProcess):
     assert set(GetRequires().cmake()) == {"cmake>=3.15"}
     assert set(GetRequires().ninja()) == {*ninja}
 
-
+@pytest.mark.usefixtures("setup_macos_tools")
 def test_get_requires_parts_uneeded(fp: FakeProcess):
     fp.register(
         [Path("cmake/path"), "-E", "capabilities"],
@@ -43,7 +43,7 @@ def test_get_requires_parts_uneeded(fp: FakeProcess):
     assert set(GetRequires().cmake()) == set()
     assert set(GetRequires().ninja()) == {*ninja}
 
-
+@pytest.mark.usefixtures("setup_macos_tools")
 def test_get_requires_parts_settings(fp: FakeProcess):
     fp.register(
         [Path("cmake/path"), "-E", "capabilities"],
@@ -53,7 +53,7 @@ def test_get_requires_parts_settings(fp: FakeProcess):
     assert set(GetRequires.from_config_settings(config).cmake()) == {"cmake>=3.20"}
     assert set(GetRequires.from_config_settings(config).ninja()) == {*ninja}
 
-
+@pytest.mark.usefixtures("setup_macos_tools")
 def test_get_requires_parts_pyproject(
     fp: FakeProcess, monkeypatch: pytest.MonkeyPatch, tmp_path: Path
 ):
@@ -72,7 +72,7 @@ def test_get_requires_parts_pyproject(
     assert set(GetRequires().cmake()) == {"cmake>=3.21"}
     assert set(GetRequires().ninja()) == {*ninja}
 
-
+@pytest.mark.usefixtures("setup_macos_tools")
 def test_get_requires_parts_pyproject_old(
     fp: FakeProcess, monkeypatch: pytest.MonkeyPatch, tmp_path: Path
 ):
@@ -93,7 +93,7 @@ def test_get_requires_parts_pyproject_old(
     assert set(GetRequires().cmake()) == {"cmake>=3.21"}
     assert set(GetRequires().ninja()) == {*ninja}
 
-
+@pytest.mark.usefixtures("setup_macos_tools")
 def test_get_requires_for_build_sdist(fp: FakeProcess):
     fp.register(
         [Path("cmake/path"), "-E", "capabilities"],
@@ -101,7 +101,7 @@ def test_get_requires_for_build_sdist(fp: FakeProcess):
     )
     assert set(get_requires_for_build_sdist({})) == set()
 
-
+@pytest.mark.usefixtures("setup_macos_tools")
 def test_get_requires_for_build_sdist_cmake(fp: FakeProcess):
     expected = {"cmake>=3.15", *ninja}
     fp.register(
@@ -110,7 +110,7 @@ def test_get_requires_for_build_sdist_cmake(fp: FakeProcess):
     )
     assert set(get_requires_for_build_sdist({"sdist.cmake": "True"})) == expected
 
-
+@pytest.mark.usefixtures("setup_macos_tools")
 def test_get_requires_for_build_wheel(fp: FakeProcess):
     expected = {"cmake>=3.15", *ninja}
     fp.register(
@@ -119,7 +119,7 @@ def test_get_requires_for_build_wheel(fp: FakeProcess):
     )
     assert set(get_requires_for_build_wheel({})) == expected
 
-
+@pytest.mark.usefixtures("setup_macos_tools")
 def test_get_requires_for_build_wheel_pure(fp: FakeProcess):
     fp.register(
         [Path("cmake/path"), "-E", "capabilities"],
@@ -127,7 +127,7 @@ def test_get_requires_for_build_wheel_pure(fp: FakeProcess):
     )
     assert set(get_requires_for_build_wheel({"wheel.cmake": "False"})) == set()
 
-
+@pytest.mark.usefixtures("setup_macos_tools")
 def test_get_requires_for_build_editable(fp: FakeProcess):
     expected = {"cmake>=3.15", *ninja}
     fp.register(
@@ -136,7 +136,7 @@ def test_get_requires_for_build_editable(fp: FakeProcess):
     )
     assert set(get_requires_for_build_editable({})) == expected
 
-
+@pytest.mark.usefixtures("setup_macos_tools")
 def test_get_requires_for_build_editable_pure(fp: FakeProcess):
     fp.register(
         [Path("cmake/path"), "-E", "capabilities"],
diff --git a/tests/test_program_search.py b/tests/test_program_search.py
index 319a8e3..092da6d 100644
--- a/tests/test_program_search.py
+++ b/tests/test_program_search.py
@@ -36,8 +36,13 @@ def test_get_ninja_programs_cmake_module(monkeypatch):
 # return an relative path when the path is non-existent. But we don't care about
 # that case, so we'll just do the same thing as the search does.
 
-
+@pytest.mark.usefixtures("setup_macos_tools")
 def test_get_cmake_programs_all(monkeypatch, fp):
+    # Mock the function to always return False (not x86)
+    monkeypatch.setattr(
+        "scikit_build_core.program_search._macos_binary_is_x86",
+        lambda path: False
+    )
     monkeypatch.setattr("shutil.which", lambda x: x)
     cmake_path = Path("cmake")
     cmake3_path = Path("cmake3")
@@ -64,9 +69,14 @@ def test_get_cmake_programs_all(monkeypatch, fp):
     assert best2
     assert best2.path.name == "cmake"
 
-
+@pytest.mark.usefixtures("setup_macos_tools")
 def test_get_ninja_programs_all(monkeypatch, fp):
     monkeypatch.setattr("shutil.which", lambda x: x if "ninja" in x else None)
+    # Mock the function to always return False (not x86)
+    monkeypatch.setattr(
+        "scikit_build_core.program_search._macos_binary_is_x86",
+        lambda path: False
+    )
     ninja_path = Path("ninja")
     ninja_build_path = Path("ninja-build")
     fp.register([ninja_path, "--version"], stdout="1.10.1.git.kitware.jobserver-1")
@@ -86,7 +96,7 @@ def test_get_ninja_programs_all(monkeypatch, fp):
     assert best2
     assert best2.path.name == "ninja"
 
-
+@pytest.mark.usefixtures("setup_macos_tools")
 def test_get_cmake_programs_malformed(monkeypatch, fp, caplog):
     caplog.set_level(logging.WARNING)
     monkeypatch.setattr("shutil.which", lambda x: x)
